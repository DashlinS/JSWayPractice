const number1 = Number(prompt("Pick the First Number"));
const number2 = Number(prompt("Pick the Second Number"));

if (number1 !== number2) {
  console.log('These are not the same numbers!');
}

else {
  console.log('These numbers are the same!');
}

