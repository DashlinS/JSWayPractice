alert('Find the circumference and Area of a circle!')

let num = prompt('What is the radius?')
let circ = 2 * Math.PI * num;
let area = Math.PI * num ** 2;

function calculate(circ) {
  return circ;
}

function calculate(area) {
  return area;
}
console.log(calculate(circ));
console.log(calculate(area));